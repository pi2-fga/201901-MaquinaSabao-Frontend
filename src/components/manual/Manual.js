import React, { Component } from 'react';
import { Container, Content, Title } from 'native-base';

export default class Manual extends Component {
  render() {
    return (
      <Title style={{ color: 'black', marginTop: '5%', marginBottom: '10%' }}>Manual de Uso</Title>
    );
  }
}
